name: official_extensions
description: Yukino official extensions
version: 0.0.0

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  extensions_test:
    path: ./packages/extensions_test

dev_dependencies:
  devx:
    git:
      url: https://github.com/yukino-app/yukino.git
      ref: next
      path: packages/devx
  lint: ^1.0.0
  path: ^1.8.0
