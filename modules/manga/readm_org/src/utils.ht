
    static const baseUrl = 'https://readm.org';

    defaultHeaders := {
        'User-Agent': Http.defaultDesktopUserAgent(),
        'Referer': baseUrl,
    }

    defaultLocale := Locale.parse('en');

    getSearchUrl := ->  {
        return Url.ensure('{baseUrl}/service/search');
    }

    appendBaseUrl := -> route {
        return Url.ensure('{baseUrl}{route}');
    }
}
